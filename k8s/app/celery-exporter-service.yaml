apiVersion: v1
kind: Service
metadata:
  name: celery-exporter
  namespace: rag-project
spec:
  selector:
    app: celery-exporter
  ports:
    - port: 9808
      targetPort: 9808
      nodePort: 30908
  type: NodePort
