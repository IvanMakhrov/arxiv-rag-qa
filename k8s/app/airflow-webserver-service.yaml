apiVersion: v1
kind: Service
metadata:
  name: airflow-webserver
  namespace: rag-project
spec:
  selector:
    app: airflow-webserver
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30808
  type: NodePort
