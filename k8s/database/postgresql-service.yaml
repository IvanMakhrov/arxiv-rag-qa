apiVersion: v1
kind: Service
metadata:
  name: postgresql-ha
  namespace: rag-project
  labels:
    app: postgresql
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: postgresql
  type: ClusterIP
