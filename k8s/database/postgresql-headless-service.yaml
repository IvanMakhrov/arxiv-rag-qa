apiVersion: v1
kind: Service
metadata:
  name: postgresql-headless
  namespace: rag-project
  labels:
    app: postgresql
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  clusterIP: None
  selector:
    app: postgresql
